class Solution {
    public int maxProfit(int[] a) {

        int n = a.length;

        int min = a[0];

        int earn = 0;

        for(int i = 0; i < n; ++i) {

            earn += Math.max(0, a[i] - min);
            min = a[i];

        }
        
        return earn = earn;

    }
}
